app:
  title: Scaffolded Backstage App
  baseUrl: http://localhost:3000

organization:
  name: Lam Nguyen

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: http://localhost:3000
    methods: [GET, POST, PUT, DELETE]
    credentials: true
  database:
    client: pg
    connection:
      host: localhost
      port: 5432
      user: backstage
      password: backstage
integrations:
  github:
    - host: github.com

      token: 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


techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

auth:
  environment: development
  providers:
    github:
      development:
        clientId: 79d94ad95c4b80ee109e
        clientSecret: 24c463d0e948006e0b36363c05a5c10057ff8092

scaffolder:
  defaultAuthor:
    name: Lam Nguyen
    email: vanlamnguyen2110@gmail.com
  defaultCommitMessage: "Commit from Backstage"
catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location]
  locations:
    - type: file
      target: ../../examples/entities.yaml

    - type: url
      target: https://github.com/alternativevn/my-backstage-templates/blob/main/GitHub%20CI/template.yaml

    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

    - type: url
      target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/all.yaml

    # - type: url
    #   target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/acme-corp.yaml
    #   rules:
    #     - allow: [User, Group]
